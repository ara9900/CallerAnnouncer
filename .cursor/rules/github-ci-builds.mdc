---
description: Always build APK via GitHub Actions; no local Gradle builds; install on phone
alwaysApply: true
---

# CallerAnnouncer build policy

- After meaningful code changes: commit, push to `master`/`main`, and let GitHub Actions build the debug APK.
- Do **not** run local `./gradlew assembleDebug` (or similar) unless the user explicitly asks — this machine is weak.
- After push, wait for the Actions run and share:
  - Actions run URL
  - Artifact name: `CallerAnnouncer-Debug-APK`
- **Always install on the connected phone** after a successful Actions build:
  1. `gh run download <run-id> -n CallerAnnouncer-Debug-APK -D <temp-dir>`
  2. `adb install -r <temp-dir>/app-release.apk` (confirm `adb devices` first)
  3. Tell the user the installed `versionName` in Persian
- Speak with the user in Persian for this project.
